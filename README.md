# Shell is Terrible
Shell is Terrible is a simple shell created for Windows. 

# Builtin Commands
1. ```exit```
- Exits the shell

2. ```banner```
- clears the screen and shows the banner of the shell

# Features
- Interactive prompt loop
- Parse command + arguments
- Built-in commands
- Run external programs using spawnvp (for Windows)
- Handle invalid commands gracefully